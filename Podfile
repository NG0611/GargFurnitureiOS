platform :ios, '15.0'

target 'GargFurniture' do
  use_frameworks!
  use_modular_headers!

  # Firebase (pods version)
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Functions'

  # Razorpay
  pod 'razorpay-pod', '~> 1.5.0'
end